[workspace]
resolver = "2"
members = [
    "crates/wp-bidi",
    "crates/wp-break",
    "crates/wp-normal",
    "crates/wp-pdf",
    "crates/wp-segment",
    "crates/wp-cli",
    "crates/wp-deflate",
    "crates/wp-docx",
    "crates/wp-font",
    "crates/wp-image",
    "crates/wp-shape",
    "crates/wp-svg",
    "crates/wp-raster",
    "crates/wp-shell",
    "crates/wp-app",
    "crates/wp-layout",
    "crates/wp-opc",
    "crates/wp-xml",
    "crates/wp-zip",
]

[workspace.package]
version = "0.0.1"
edition = "2021"
# Raised from 1.75 when the font reader started using Option::is_none_or.
# The declared minimum has to match what the code actually needs, or it is
# a claim nobody has checked.
rust-version = "1.82"
license = "MIT"
repository = ""

[workspace.lints.rust]
unsafe_code = "warn"
missing_debug_implementations = "warn"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[profile.dev]
# Font parsing and rasterization are unbearably slow in a fully unoptimized
# build, so debug builds keep a light optimization level.
opt-level = 1
